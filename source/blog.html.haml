---
pageable: true
title: gorilla software blog
layout: layouts/blog_summary
---

- page_articles.each_with_index do |article, i|
  %article.blog-entry
    %h2
      = link_to article.title, article
      %span= article.date.strftime('%b %e')
    = article.summary(250)
  .separator
    .line.line-1

- if paginate
  %hr
  .pagination-centered
    %ul.pagination
      - if prev_page
        %li.arrow
          = link_to '«', prev_page
      %li
        Page #{page_number} of #{num_pages}
      - if next_page
        %li.arrow
          = link_to '»', next_page
